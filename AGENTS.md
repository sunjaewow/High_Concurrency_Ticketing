# 1. Think Before Coding

추측하지 말 것. 헷갈리는 부분을 숨기지 말 것. 선택지와 트레이드오프를 드러낼 것. 문제를 해결하기 위한 최소한으로 작성할 것.

구현을 시작하기 전에:

- 내가 전제로 삼은 내용을 명확히 말한다.
- 확실하지 않은 부분이 있으면 질문한다.
- 여러 해석이 가능하면 임의로 하나를 고르지 말고, 가능한 해석들을 제시한다.
- 더 단순한 방법이 있다면 말한다.
- 필요하다면 사용자의 요청에 반대 의견도 낸다.
- 불명확한 부분이 있으면 멈추고, 무엇이 헷갈리는지 말한 뒤 질문한다.

# 2. Simplicity First

문제를 해결하는 최소한의 코드만 작성한다. 추측성 기능은 만들지 않는다.

- 요청받은 것 이상의 기능을 만들지 않는다.
- 한 번만 쓰이는 코드를 위해 추상화를 만들지 않는다.
- 요청받지 않은 “유연성”이나 “설정 가능성”을 추가하지 않는다.
- 실제로 발생할 수 없는 상황을 위한 에러 처리를 넣지 않는다.
- 200줄로 작성한 코드가 50줄로도 가능하다면 다시 단순하게 작성한다.

스스로에게 이렇게 질문한다.
“시니어 개발자가 봤을 때 이 코드가 과하게 복잡하다고 생각하지 않을까?”
그렇다면 단순화한다.

# 3. Surgical Changes

반드시 필요한 부분만 건드린다. 너가 만든 변경으로 생긴 문제만 정리한다.

기존 코드를 수정할 때:

- 관련 없는 주변 코드, 주석, 포맷을 “개선”하지 않는다.
- 고장 나지 않은 코드를 리팩토링하지 않는다.
- 내가 다르게 하고 싶더라도 기존 코드 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 말만 하고, 삭제하지 않는다.

내 변경으로 인해 더 이상 사용되지 않는 코드가 생긴 경우:

- 내가 만든 변경 때문에 사용되지 않게 된 import, 변수, 함수는 제거한다.
- 기존에 이미 있던 죽은 코드는 요청받지 않았다면 제거하지 않는다.

기준:
변경된 모든 줄은 사용자의 요청과 직접적으로 연결되어 있어야 한다.

# 4. Goal-Driven Execution

성공 기준을 정하고, 검증될 때까지 반복한다.

예시:

- “검증 로직 추가”
  → “잘못된 입력에 대한 테스트를 작성하고, 그 테스트를 통과시킨다.”
- “버그 수정”
  → “버그를 재현하는 테스트를 작성하고, 그 테스트를 통과시킨다.”
- “X 리팩토링”
  → “리팩토링 전후로 테스트가 통과하는지 확인한다.”

여러 단계가 필요한 작업이라면 짧은 계획을 먼저 작성한다.

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

명확한 성공 기준이 있으면 독립적으로 반복하며 작업할 수 있다.
반대로 “동작하게 만들기”처럼 기준이 약하면 계속 사용자에게 확인해야 한다.

# Security rules

- 민감한 정보는 환경 변수로 관리한다.
- 내 허락 없이 파일 삭제하지 마라.

# Working rules

- 구현하기 전에 실행계획을 반드시 검토를 받는다.
- Lombok 활용해서 코드 복잡성을 줄인다.
- 클린 아키텍처로 패키지 구조를 구성한다.
- 단위 테스트로 테스트를 진행한다.
- 코드 변경을 하면 커밋을 찍지말고 커밋 내용을를 이해하기 쉽고 한국어로 구체적으로 작성하여 추천해준다. 예시 : "feat : 로그인 구현"


